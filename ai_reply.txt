Now I have a full picture. Let me rewrite the README with badges, an assets section, and mermaid visualizations.
Let me fix one fragile badge and verify the mermaid syntax renders
Redid `README.md` with badges, bundled-asset documentation, and mermaid visualizations:

- **Badges** — toolbar of shields.io badges (Java 26, LWJGL 3.3.6, OpenGL 3.3, Maven, JOML, platform) plus live GitHub badges (repo size, last commit, top language, open issues), with a table explaining each.
- **Assets** — new *Bundled Assets* section listing all 5 GLB models + texture atlas with paths, sizes, roles, and attribution (CesiumMan CC-BY 4.0). Reflected real paths (`source/`, `assets/passengers/`) instead of the stale `traffic_slam_2_map.glb` name.
- **Visualizations** — 5 mermaid diagrams: package-layer architecture, runtime flow, game state machine, and passenger state machine, plus a corrected project tree matching the actual `game.core`, `game.scoring`, and asset directories.
- Fixed the jar name in build commands and added a *Related Documents* section.

Changed: `README.md`

You're right — the README was never actually changed. The last commits were only the borevibe workflow and the MariaDB diagram; the modified files were `full_prompt.txt`, not `README.md`. Let me actually do the
Verified: the README redo was never actually committed. Only the workflow file and `full_prompt.txt` changed. Now doing it for
Let me verify a few facts I referenced before committing.
Fixing two inaccuracies (JOML version,
Now committing and pushing the README re
You're right — it was never actually changed. The prior session only described it; nothing was committed (`03c5971` was just the workflow file). I verified the real state, recreated the README with accurate facts, and pushed it now.

**Pushed:** commit `d6fcc22` → `master` (README.md, +243/−315)

What's actually in it:

- **Badges** — Java 26, LWJGL 3.3.6, OpenGL 3.3 Core, JOML 1.10.8, Maven, Linux + live GitHub stats, with an explanatory table.
- **Bundled Assets** — all 5 GLB models with real paths/sizes/roles and Cesium CC-BY-4.0 attribution (the actual `source/*.glb`, `assets/passengers/*`, traffic BWM model), plus the extracted `gltf_embedded_*` textures.
- **Visualizations** — 5 mermaid diagrams: package-layer architecture, runtime sequence, game state machine, passenger state machine, and a corrected project tree (reflects real `core/` and `scoring/` packages).
- Also fixed the stale jar name (`city-racer-new-variation-ref-1.0.jar`), scoreboard references, and corrected the claimed file tree.

Changed: README.md

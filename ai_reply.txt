The `MARIA_DESIGN_DIAGRAM.md` file doesn't actually exist — the previous session claimed to create it but never wrote it. Let me recreate it,
Now commit and push
Pushed. The `MARIA_DESIGN_DIAGRAM.md` file from the prior session was never actually written (only described), so I recreated it from that description — mermaid ER diagram + full MariaDB DDL — then committed and pushed.

**Pushed:** `MARIA_DESIGN_DIAGRAM.md` (commit `2c85816` → `master`)
